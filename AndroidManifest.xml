<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="rot.activity"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher" android:theme="@android:style/Theme.Black.NoTitleBar">
        <activity android:name="MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="TripActivity" android:label="@string/app_name" />
        <activity android:name="SetReminderActivity" android:label="@string/app_name" />
        <activity android:name="RemindActivity" android:label="@string/app_name" />
        <activity android:name="TimeToGoActivity" android:label="@string/app_name" />
        <activity android:name="AboutActivity" android:label="@string/app_name" />
        <activity android:name="StopMapActivity" android:label="@string/app_name" />
        <uses-library android:name="com.google.android.maps" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-sdk android:minSdkVersion="7" />
</manifest>
